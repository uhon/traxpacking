<?php

$server = new Zend_Json_Server();
//$server->setClass('');
$smd = $server->getServiceMap();

header("Content-Type: text/html; charset=UTF-8");
echo "<?xml version=\"1.0\" encoding=\"utf-8\"?>\n";

?><?=$this->doctype('XHTML1_TRANSITIONAL')?>
<html xml:lang="en-US" lang="en-US" xmlns="http://www.w3.org/1999/xhtml">
<head>
	<meta name="" content="<?=$this->escape($this->title)?>" />
	<meta name="keywords" content="<?=$this->escape(implode(',', array_merge(array(
		'',
	), (array)$this->keywords)))?>" />
	<meta name="description" content="<?=$this->escape($this->description)?>" />
    <?php
        echo $this->jQuery()
            /*->setVersion('1.7.1')->enable()
            ->setUiVersion('1.8.17')->uiEnable()
            //->addStylesheet('http://ajax.googleapis.com/ajax/libs/jqueryui/1.8/themes/ui-darkness/jquery-ui.css')*/
            ->addStylesheet("/js/jquery/3rd_party/supersized/slideshow/css/supersized.css")
            ->addStylesheet("/js/jquery/3rd_party/supersized/slideshow/themesupersized.shutter.css")
    ?>
    <?=$this->headLink()?>
    <?=$this->headScript()?>
    <!--[if lt IE 9]>
      <script type="text/javascript" src="/js/3rd_party/excanvas.js"></script>
    <![endif]-->
    <script type="text/javascript" src="/js/3rd_party/jquery.mb.bgndGallery/inc/mb.bgndGallery.js"></script>


	<title>Traxpacking - Backpacking by GPS</title>
</head>
<body>
    <?php
        /*$authUrl = array(
            'module' => 'default',
            'controller' => 'auth',
            'action' => 'logout',
        );

        if(Zend_Auth::getInstance()->hasIdentity()) {
            echo '<span><a href="' . $this->url($authUrl, null, true) . '">logout</a></span>';
        } else {
            $authUrl['action'] = 'login';
            echo '<span><a href="' . $this->url($authUrl, null, true) . '">login</a></span>';
        }*/
    ?>
    <div id="playground">
        <?= $this->layout()->content; ?>
    </div>
</body>
</html>
