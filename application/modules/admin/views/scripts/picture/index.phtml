<?php
    if(!empty($this->newPictures)) {
        echo '<h2>New Pictures:</h2>';
        echo $this->table($this->newPictures, array('Filename', 'Size', 'Date & Time', ''));
    }
    echo "<br/>";
    if(!empty($this->unassignedPictures)) {
        echo '<h2>Unassigned Pictures:</h2>';
        echo $this->table($this->unassignedPictures, array('Filename', 'Date & Time', ''));
    }